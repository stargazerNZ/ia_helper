# UI module: all GTK4/libadwaita code lives here.
#
# Portability rule: libadwaita usage is kept to layout chrome
# (ApplicationWindow, HeaderBar, ToolbarView, toasts, status pages) so a
# future Windows build can swap it for plain GTK without touching core/.
